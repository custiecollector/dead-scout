# DeadScout capture helpers

The public DeadScout build keeps helper guidance narrow: use operator-provided RTL/SDR tools, `rtl_tcp`, local import files, and 802.15.4/sniffer logs.

No third-party Windows packet-driver bootstrap, external analyzer launcher, or OS scan helper is bundled in this public package.
